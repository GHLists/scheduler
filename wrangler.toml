name = "ghlists-dispatcher"
main = "src/index.js"
compatibility_date = "2026-09-01"

# Cloudflare cron triggers run in UTC.
# Dispatches all three GHLists workflows once per hour at :18.
[triggers]
crons = ["18 * * * *"]
