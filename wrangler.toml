name = "ghlists-dispatcher"
main = "src/index.js"
compatibility_date = "2026-09-01"

# Cloudflare cron triggers run in UTC.
# Dispatches all four GHLists workflows once per hour at :07.
[triggers]
crons = ["7 * * * *"]
